<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="newStockMasterMapper">			
	
	<insert id="newStockMaster">
		insert into stock_master(id,code,units,mbflag,lastcurcostdate,actualcost,lastcost,materialcost,labourcost,overheadcost,lowestlevel,discontinued,controlled,eoq,volume,kgs,barcode,discountcategory,taxcatid,serialised,appendfile,perishable,decimalplaces,nextserialno,pansize,shrinkfactor) 
		values (#{id},#{code},#{units},#{mbflag},#{lastcurcostdate},#{actualcost},#{lastcost},#{materialcost},#{labourcost},#{overheadcost},#{lowestlevel},#{discontinued},#{controlled},#{eoq},#{volume},#{kgs},#{barcode},#{discountcategory},#{taxcatid},#{serialised},#{appendfile},#{perishable},#{decimalplaces},#{nextserialno},#{pansize},#{shrinkfactor});
	</insert>

</mapper>
