<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="newAppUsersMapper">			
	
	<insert id="newAppUsers">
		insert into app_users(username,password,name,email,phone,mobile,enabled,token) 
		values (#{username},#{password},#{name},#{email},#{phone},#{mobile},#{enabled},#{token});
	</insert>

</mapper>
