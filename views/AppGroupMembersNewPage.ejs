<!DOCTYPE html>
<html>
<head lang="en">
<% include head %>
</head>
<body>
<% include side-nav %>
<div id="main">
<% include header-nav %>
<div class="container-fluid">
<section id="main-content">

    <div style="width:40%">
    <h3><%=resource.Title%></h3>
    <form class="form-horizontal" action="/newAppGroupMembers" method="post">
        <table class="table table-striped">
            <tr class="heading-tr">
                <th><%=resource.Heading%></th>
                <th style="text-align:right"><%=statusMessage%></th>
            </tr>
            <tr>
                <td><%=resource.GroupId.label%></td>
                <td><input type="text" class="form-control" name="groupId" value="<%=record.groupId%>"/></td>
            </tr><tr>
                <td><%=resource.GroupMemberId.label%></td>
                <td><input type="text" class="form-control" name="groupMemberId" value="<%=record.groupMemberId%>"/></td>
            </tr><tr>
                <td><%=resource.Username.label%></td>
                <td><input type="text" class="form-control" name="username" value="<%=record.username%>"/></td>
            </tr>
        
        </table>
        <button type="submit" class="btn btn-default">Add</button>
        <% if(statusCode == 200) { %>
        <button type="button" class="btn btn-default"><a href="/<%=session.previouspath%>">Back</a></button>
        <% }else{ %>
        <button type="button" class="btn btn-default" onclick="javascript:window.history.back()">Cancel</button>
        <% } %>
    </form>
    </div>

</section>
</div>
</div>
<% include footer %>
</body>
</html>
