<!DOCTYPE html>
<html>
<head lang="en">
<% include head %>
</head>
<body>
<% include side-nav %>
<div id="main">
<% include header-nav %>
<div class="container-fluid">
<section id="main-content">

    <ul class="nav nav-tabs" id="myTab" role="tablist">
      <li class="nav-item">
        <a class="nav-link active" id="home-tab" data-toggle="tab" href="#home" role="tab" aria-controls="home" aria-selected="true"><h4><%=resource.Heading%></h4></a>
      </li>
    </ul>
    
    <div class="tab-content" id="myTabContent">

        <div class="tab-pane fade in active" id="home" role="tabpanel" aria-labelledby="home-tab">	
            <div style="width:40%">
            <form class="form-horizontal">
                <table class="table table-striped">
                    <tr class="heading-tr">
                        <th> <%=resource.Heading%></th>
                        <th></th>
                    </tr>
                    <tr>
                        <td><%=resource.Id.label%></td>
                        <td><%=record.id%></td>						
                    </tr><tr>
                        <td><%=resource.Code.label%></td>
                        <td><%=record.code%></td>						
                    </tr><tr>
                        <td><%=resource.Name.label%></td>
                        <td><%=record.name%></td>						
                    </tr><tr>
                        <td><%=resource.Description.label%></td>
                        <td><%=record.description%></td>						
                    </tr><tr>
                        <td><%=resource.Type.label%></td>
                        <td><%=record.type%></td>						
                    </tr><tr>
                        <td><%=resource.AccStock.label%></td>
                        <td><%=record.acc_stock%></td>						
                    </tr><tr>
                        <td><%=resource.AccAdjgl.label%></td>
                        <td><%=record.acc_adjgl%></td>						
                    </tr><tr>
                        <td><%=resource.AccPurchase.label%></td>
                        <td><%=record.acc_purchase%></td>						
                    </tr><tr>
                        <td><%=resource.AccMaterial.label%></td>
                        <td><%=record.acc_material%></td>						
                    </tr><tr>
                        <td><%=resource.AccWip.label%></td>
                        <td><%=record.acc_wip%></td>						
                    </tr>
                
                </table>
                <% if(session.user_acl=='ROLE_ADMIN') { %>
                <button type="submit" class="btn btn-default"><a href="/writeProductAccount?id=<%=record.id%>">Edit</a></button>
                <button type="submit" class="btn btn-default"><a href="/deleteProductAccount?id=<%=record.id%>">Delete</a></button>
                <% } %>
                <button type="button" class="btn btn-default" onclick="javascript:window.history.back()">Back</button>
            </form>
            </div>
        </div>
        
    </div>

</section>
</div>
</div>
<% include footer %>
</body>
</html>
