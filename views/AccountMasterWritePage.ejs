<!DOCTYPE html>
<html>
<head lang="en">
<% include head %>
</head>
<body>
<% include side-nav %>
<div id="main">
<% include header-nav %>
<div class="container-fluid">
<section id="main-content">

    <div style="width:40%">
    <h3><%=resource.Title%></h3>
    <form class="form-horizontal" action="/writeAccountMaster" method="post">
        <table class="table table-striped">
            <tr class="heading-tr">
                <th><%=resource.Heading%></th>
                <th style="text-align:right"><%=statusMessage%></th>
            </tr>
            <tr>
                <td><%=resource.Id.label%></td>
                <td><input type="text" class="form-control" name="id" value="<%=record.id%>"/></td>
            </tr><tr>
                <td><%=resource.AccCode.label%></td>
                <td><input type="text" class="form-control" name="accCode" value="<%=record.acc_code%>"/></td>
            </tr><tr>
                <td><%=resource.AccName.label%></td>
                <td><input type="text" class="form-control" name="accName" value="<%=record.acc_name%>"/></td>
            </tr><tr>
                <td><%=resource.AccDescription.label%></td>
                <td><input type="text" class="form-control" name="accDescription" value="<%=record.acc_description%>"/></td>
            </tr><tr>
                <td><%=resource.AccGroupId.label%></td>
                <td><input type="text" class="form-control" name="accGroupId" value="<%=record.acc_group_id%>"/></td>
            </tr>
        
        </table>
        <button type="submit" class="btn btn-default">Update</button>
        <% if(statusCode == 200) { %>
        <button type="button" class="btn btn-default""><a href="/<%=session.previouspath%>">Back</a></button>
        <% }else{ %>
        <button type="button" class="btn btn-default" onclick="javascript:window.history.back()">Cancel</button>
        <% } %>
    </form>
    </div>

</section>
</div>
</div>
<% include footer %>
</body>
</html>
